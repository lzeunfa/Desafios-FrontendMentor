Primeiro deletei a parte inutil de incrementar e decrementar

-Preciso ajeitar as classes ao invés de ids
-Preciso colocar as functions e passar parametros de valor e nome do item
-Ajeitar todos os btns que eu coloquei
-excluir os values dos btns
-Ajustar algo do js que recebe a alteração das imagens de cartw buyw algo do tipo

-Preciso criar divs que apareçam com o nome do item e apenas ir alterando a quantidade dentro da div e o valor
-Quando apertar no x da div ele exclui os dados e o botao volta ao normal tambem